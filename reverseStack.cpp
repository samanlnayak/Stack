# include<iostream>
# include<stack>
using namespace std;
void reverseStack(stack<int>&st){
    while(st.size()>0){
        int x=st.top();
        
        
    }
}
int main(){
    stack<int>st;
    st.push(10);
    st.push(20);
    st.push(30);
    st.push(40);

    
}